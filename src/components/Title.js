import React from "react";

const Title = props => (
  <div>
    <h1 className="title-container__title">Weather App</h1>
    <p className="title-container__subtitle">
      Find out temperature, humidity and other weather conditions...
    </p>
  </div>
);
export default Title;
